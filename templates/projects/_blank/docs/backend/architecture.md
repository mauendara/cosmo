# Backend Architecture

<!-- Schema-only template (Cosmo `_blank` project template). Fill in each
     section as decisions are made; keep entries factual and current rather
     than narrating history -- git history and the event log already cover
     "what happened". See spec 11 / CLAUDE.md for the append/reconcile
     convention this file follows once a project is underway. -->

## Layers

<!-- The application's major layers (e.g. controller/service/repository) and
     what belongs in each. -->

## Module boundaries

<!-- How the codebase is split into modules/packages, and the rule for what
     goes where. -->

## Key dependencies

<!-- Frameworks and libraries this backend is built on, and why each was
     chosen over the alternatives, where that reasoning isn't obvious. -->

## Cross-cutting concerns

<!-- Logging, config, error handling (see error-handling.md for detail),
     transactions -- whatever spans every module. -->
