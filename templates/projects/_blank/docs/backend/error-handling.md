# Backend Error Handling

<!-- Schema-only template (Cosmo `_blank`). -->

## Error taxonomy

<!-- The categories of error this backend distinguishes (e.g. validation,
     not-found, conflict, upstream-dependency) and the HTTP status/response
     shape each maps to. -->

## Response format

<!-- The exact shape of an error response body, so frontend and backend agree
     without reading each other's source. -->

## Logging vs. surfacing

<!-- What gets logged server-side only vs. what's safe to return to a client
     -- especially where that boundary isn't obvious (stack traces, internal
     ids, third-party error text). -->
