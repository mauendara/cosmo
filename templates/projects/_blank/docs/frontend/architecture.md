# Frontend Architecture

<!-- Schema-only template (Cosmo `_blank`). -->

## Structure

<!-- How the app is organized (routes, feature folders, shared components)
     and the rule for what goes where. -->

## Data flow

<!-- How data reaches a component: fetching, caching, and how server state
     is distinguished from local UI state (see state-management.md). -->

## Key dependencies

<!-- Framework/build tooling and why, where that reasoning isn't obvious. -->
