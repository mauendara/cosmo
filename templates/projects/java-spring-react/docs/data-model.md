# Data Model

## Core entities

<!-- Fill in as the schema exists: the central entities and how they relate
     (a short ER-style list is usually enough -- the migrations are the
     actual source of truth for exact columns/types). -->

## Ownership and lifecycle

<!-- For each core entity: what creates it, what can mutate it, what (if
     anything) can delete it, and any invariant that must hold across its
     lifecycle (e.g. "an Order's total is recomputed, never edited directly"). -->

## Known constraints

<!-- Uniqueness rules, foreign-key/referential-integrity expectations, and
     anything a migration must be careful not to violate. Keep this in sync
     with the actual migrations -- if they disagree, the migrations win and
     this file is stale and needs fixing. -->
